class Solution {
public:
   char firstNotRepeatingChar(string s) {
	if (s.size() < 1) return '#';
	unordered_map<char, int> map;

	for (auto c : s) {
		map[c]++;
	}

	for (auto c : s) {
		if (map[c] == 1) return c;
	}

	return '#';
}
};
