class Solution {
public:
    void deleteNode(ListNode* node) {
        //先用下一个节点覆盖待删除的节点 再把下一个节点删除
        ListNode* p = node->next;
        node->val = p->val;
        node->next = p->next;
        //删除最后一个节点
        delete p;
    }
};
