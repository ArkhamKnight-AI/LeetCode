class Solution {
public:
    string leftRotateString(string str, int n) {
	if (n == 0 || str.size() < 1) return str;

	for (int index = 0; index < n; index++) {
		str += str[index];
	}
	return str.substr(n, str.size());
}

};
