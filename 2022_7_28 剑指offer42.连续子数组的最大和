class Solution {
public:
    int maxSubArray(vector<int>& nums) {
	int maxSub = INT_MIN; //记录之前最大的累加和
	int cur = 0; //记录当前的累加和
	for (int i = 0; i < nums.size(); i++) {
		cur += nums[i];

		maxSub = max(cur, maxSub);

		cur = cur < 0 ? 0 : cur;
	}

	return maxSub;
}
};
