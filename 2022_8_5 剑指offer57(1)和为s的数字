class Solution {
public:
    vector<int> findNumbersWithSum(vector<int>& nums, int target) {
	unordered_set<int> map;

	for (int i = 0; i < nums.size(); i++) {
		if (map.count(target - nums[i]) == 1) return vector<int>{nums[i], target - nums[i]};
		else{
		    map.insert(nums[i]);
		}
	}
}
};
