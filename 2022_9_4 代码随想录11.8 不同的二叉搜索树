int numTrees(int n) {
	vector<int> dp(n + 1);
	dp[0] = 1;

	for (int i = 1; i <= n; i++) {
		for (int j = 1; j <= i; j++) {
			//左子树规模为j-1 右子树规模为i - j 总共有i个节点
			dp[i] += dp[i - j] * dp[j - 1];
		}
	}

	return dp[n];
}
